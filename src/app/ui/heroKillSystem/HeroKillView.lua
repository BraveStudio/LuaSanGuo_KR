--@auto code head
local BaseView = require("app.baseMVC.BaseView")
local HeroKillView = class("HeroKillView", BaseView)

local Functions = require("app.common.Functions")

HeroKillView.csbResPath = "lk/csb"
HeroKillView.debug = true
--@auto code head end


--@auto code uiInit
function HeroKillView:onInitUI()

    --output list
    
    --label list
    
    --button list
    
end
--@auto code uiInit end


return HeroKillView